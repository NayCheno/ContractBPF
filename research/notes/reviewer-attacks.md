# Reviewer Attacks

- This is just cgroups: answer with effect-level attribution and minimal degrade.
- This is a verifier problem: answer with verifier-accepted policies that are locally safe but globally harmful.
- Paging hook is unsafe: answer with read-only summarized state and kernel-validated decisions.
- QEMU performance is weak: answer by separating correctness evidence from non-QEMU performance claims.

